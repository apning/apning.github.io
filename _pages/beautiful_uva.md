---
layout: page
permalink: /beautiful-uva/
title: Beautiful UVA
description:
nav: false
---

Some beautiful photos from UVA. It is possible that photos of sunsets from a particular balcony are overrepresented in this dataset. In my defense, it was a frickin' awesome view, and I love sunsets.

<!--
  Every image dropped into `assets/img/beautiful_uva/` is displayed below
  automatically — no need to list filenames here.
-->

<div class="beautiful-uva-gallery">
{% assign gallery_images = site.static_files | where_exp: "file", "file.path contains '/assets/img/beautiful_uva/'" | sort_natural: "basename" %}
{% assign image_exts = ".jpg,.jpeg,.png,.gif,.webp,.avif,.svg" | split: "," %}
{% for image in gallery_images %}
  {% assign ext = image.extname | downcase %}
  {% comment %} Skip responsive variants (e.g. name-480.webp) generated by jekyll-imagemagick {% endcomment %}
  {% assign is_variant = false %}
  {% for width in site.imagemagick.widths %}
    {% capture variant_suffix %}-{{ width }}{% endcapture %}
    {% if image.basename contains variant_suffix %}{% assign is_variant = true %}{% endif %}
  {% endfor %}
  {% if image_exts contains ext and is_variant == false %}
  <figure class="beautiful-uva-item">
    <img src="{{ image.path | relative_url }}" alt="{{ image.basename }}" loading="lazy" data-zoomable />
  </figure>
  {% endif %}
{% endfor %}
</div>

<style>
  .beautiful-uva-gallery {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 1rem;
    margin-top: 1.5rem;
  }
  .beautiful-uva-item {
    margin: 0;
  }
  .beautiful-uva-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 8px;
    display: block;
    cursor: zoom-in;
  }
</style>
